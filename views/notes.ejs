<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Libello</title>
    <link href="notes.css" rel="stylesheet">

    <!-- bootstap links -->
    <!--<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>-->
</head>
<body>
    <nav class="navbar">
      <div>
        <img style="margin: 0; padding: 0;" src="/imgs/logo.svg" alt="Libello" width="360" height="80">
      </div>
      <div style="display: flex; flex-direction: row;">
        <% if(isAdmin){ %>
            <form action="/adminpanel" method="get">
                <button class="btn btn-primary">Admin panel</button>
            </form>
        <% } %>
            <form action="/logout" method="get">
                <button class="btn btn-danger">Wyloguj</button>
            </form>  
      </div>
    </nav>
    <p style="margin-left: 80px;">Zalogowany jako: <%= userName %></p>
    <div class="container">
        <% if(notes.length === 0){ %>
            <p>Nic tu nie ma :0</p>
        <% } else { notes.forEach(n => {%>
            <div class="card" style="width: 18rem;">
              <div class="card-body">
                <h5 class="card-title"><%= n.title %></h5>
                <p class="card-text"><%= n.description %></p>
                <a href="<%= n.path %>" class="btn btn-primary">Otwórz</a>
              </div>
            </div>
        <% })}; %>
    </div>
</body>
</html>